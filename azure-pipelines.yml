trigger: none

pool: my-agent-pool

variables:
  # Replace with values from your Azure DevOps project
  organization: akash66sheoran
  project: my-project  # Adjust if project name differs from working directory
  item: 1  # Replace with the ID of your PBI
  pat: lis7xjvgyaanmgomhwonjx4ssl47pwubxkflevj3s5vpuf7dhlda  # Use a Personal Access Token with "vso.work" scope

steps:
- script: |
    authHeaderValue=$(echo -n ":$(pat)" | base64)
    curl -H "Authorization: Basic $authHeaderValue" https://dev.azure.com/akash66sheoran/my-project/_apis/wit/workitems/1?api-version=7.2-preview.3
